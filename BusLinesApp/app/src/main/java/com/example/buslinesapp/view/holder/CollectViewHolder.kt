package com.example.buslinesapp.view.holder

import android.view.View
import android.widget.LinearLayout
import com.example.buslinesapp.R

class CollectViewHolder(view: View) :BaseViewHolder(view) {

   val collectItem: LinearLayout = view.findViewById(R.id.collectLayout)
}