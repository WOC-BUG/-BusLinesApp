package com.example.buslinesapp.view.holder

import android.view.View
import android.widget.LinearLayout
import com.example.buslinesapp.R

class QueryStation2StationHolder(view: View): BaseViewHolder(view) {
    val dataItem : LinearLayout = view.findViewById(R.id.station2stationLayout)
}